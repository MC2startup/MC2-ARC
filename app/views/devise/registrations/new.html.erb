<div class='login-container'>
  <div class='row-fluid'>
    <div class='span4 offset2'>
      <div class='sign-in-container'>
        <h2>梦想从这里开始！注册</h2>
        <p>创建你的账号前，请先阅读<a href="">用户守则</a>和<a href="">隐私政策</a>。</p>
        <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
          <div class='control-group'>
            <%= f.email_field :email, :class=>'mc2-input', :autofocus => true, :placeholder => "邮箱"%>
          </div>

          <div class='control-group'>
            <%= f.password_field :password, :placeholder => "密码" %>
          </div>

          <div>
            <%= f.password_field :password_confirmation, :placeholder=>"确认密码" %></div>
          </div>

          <div class='control-group'>
            <% if devise_mapping.rememberable? -%>
              <div><%= f.check_box :remember_me %> <%= f.label :remember_me, :class=>'remember-me' %></div>
            <% end -%>
          </div>

          <div class='control-group'>
            <%= f.submit "注册MC²", :class=>"btn btn-lg btn-register" %>
          </div>
        <% end %>
      </div>

      <div class='span2'>
        <div class="login-wrap">
          <h4>已有账号？ <%= link_to '马上登录', new_user_session_path %></h4>
        </div>
      </div>
    </div>



  </div>
</div>


